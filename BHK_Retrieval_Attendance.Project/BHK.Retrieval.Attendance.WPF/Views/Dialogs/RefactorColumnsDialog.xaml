<Window x:Class="BHK.Retrieval.Attendance.WPF.Views.Dialogs.RefactorColumnsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Refactor Table Columns" 
        Height="400" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" 
                            Padding="20"
                            Background="{DynamicResource MaterialDesignPaper}"
                            Margin="10,10,10,5">
            <StackPanel>
                <TextBlock Text="🔧 Refactor Table Columns" 
                          FontSize="18"
                          FontWeight="Bold"
                          Foreground="{DynamicResource PrimaryHueMidBrush}"
                          Margin="0,0,0,5"/>
                <TextBlock Text="The table structure doesn't match the expected Employee format. Please review and fix the column structure." 
                          TextWrapping="Wrap"
                          Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" 
                      Margin="10,5,10,5"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Current Columns -->
                <materialDesign:Card Padding="15" 
                                    Margin="0,0,0,10"
                                    Background="{DynamicResource ValidationErrorBrush}">
                    <StackPanel>
                        <TextBlock Text="❌ Current Columns (Invalid)" 
                                  FontWeight="Bold"
                                  Foreground="White"
                                  Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding CurrentColumns}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Chip Content="{Binding}"
                                                        Margin="5,2"
                                                        Background="White"
                                                        Foreground="{DynamicResource ValidationErrorBrush}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Expected Columns -->
                <materialDesign:Card Padding="15" 
                                    Margin="0,0,0,10"
                                    Background="{DynamicResource SecondaryHueMidBrush}">
                    <StackPanel>
                        <TextBlock Text="✅ Expected Columns (Employee Format)" 
                                  FontWeight="Bold"
                                  Foreground="White"
                                  Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding ExpectedColumns}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Chip Content="{Binding}"
                                                        Margin="5,2"
                                                        Background="White"
                                                        Foreground="{DynamicResource SecondaryHueMidBrush}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Warning -->
                <materialDesign:Card Padding="15" 
                                    Background="{DynamicResource MaterialDesignLightBackground}"
                                    BorderBrush="{DynamicResource PrimaryHueMidBrush}"
                                    BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Warning" 
                                               Foreground="{DynamicResource PrimaryHueMidBrush}"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"/>
                        <TextBlock Text="Refactoring will restructure the table to match Employee format. Existing data may be lost." 
                                  TextWrapping="Wrap"
                                  VerticalAlignment="Center"
                                  Foreground="{DynamicResource MaterialDesignBody}"/>
                    </StackPanel>
                </materialDesign:Card>

            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="10,5,10,10">
            
            <Button Content="❌ CANCEL" 
                    Command="{Binding CancelCommand}"
                    Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
            
            <Button Content="🔧 REFACTOR COLUMNS" 
                    Command="{Binding RefactorCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>

    </Grid>
</Window>