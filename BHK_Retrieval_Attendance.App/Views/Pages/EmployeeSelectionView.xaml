<UserControl x:Class="BHK.Retrieval.Attendance.WPF.Views.Pages.EmployeeSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <!-- Employee Item Template -->
        <DataTemplate x:Key="EmployeeItemTemplate">
            <materialDesign:Card Margin="8" Padding="16" materialDesign:ElevationAssist.Elevation="Dp2"
                               Background="{DynamicResource MaterialDesignCardBackground}">
                <materialDesign:Card.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" 
                                Command="{Binding DataContext.SelectEmployeeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                CommandParameter="{Binding}"/>
                </materialDesign:Card.InputBindings>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Avatar -->
                    <Border Grid.Column="0" 
                          Width="48" Height="48" 
                          CornerRadius="24"
                          Background="{DynamicResource PrimaryHueLightBrush}"
                          VerticalAlignment="Center">
                        <TextBlock Text="{Binding UserName, Converter={StaticResource NameToInitialsConverter}}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 FontWeight="Medium"
                                 FontSize="16"
                                 Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"/>
                    </Border>

                    <!-- Employee Info -->
                    <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding UserName}" 
                                 FontWeight="Medium"
                                 FontSize="14"
                                 Foreground="{DynamicResource MaterialDesignBody}"/>
                        
                        <TextBlock FontSize="12" 
                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                 Margin="0,2,0,0">
                            <Run Text="ID:"/>
                            <Run Text="{Binding DIN}"/>
                            <Run Text=" | "/>
                            <Run Text="{Binding DepartmentName}"/>
                        </TextBlock>
                        
                        <TextBlock Text="{Binding Comment}" 
                                 FontSize="11"
                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                 Margin="0,2,0,0"
                                 Visibility="{Binding Comment, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Status Indicators -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Active Status -->
                        <Border Background="{Binding Enable, Converter={StaticResource BooleanToStatusBrushConverter}}"
                              CornerRadius="10" Padding="8,2" Margin="0,0,4,0">
                            <TextBlock Text="{Binding Enable, Converter={StaticResource BooleanToStatusTextConverter}}"
                                     FontSize="10" FontWeight="Medium"
                                     Foreground="{DynamicResource MaterialDesignPaper}"/>
                        </Border>

                        <!-- Detail Button -->
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                              Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              CommandParameter="{Binding}"
                              ToolTip="Xem chi tiết nhân viên"
                              Width="32" Height="32">
                            <materialDesign:PackIcon Kind="ArrowRight" Width="16" Height="16"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
        </DataTemplate>

        <!-- Department Filter Button Style -->
        <Style x:Key="DepartmentFilterButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Height" Value="32"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidForegroundBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Padding="24,16" Margin="0,0,0,16" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock Text="DANH SÁCH NHÂN VIÊN" 
                             Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                             Foreground="{DynamicResource PrimaryHueMidBrush}"
                             FontWeight="Medium"/>
                    
                    <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="{DynamicResource MaterialDesignBodyLight}">
                        <Run Text="Tổng số:"/>
                        <Run Text="{Binding TotalEmployeeCount}" FontWeight="Medium"/>
                        <Run Text="| Hiển thị:"/>
                        <Run Text="{Binding FilteredEmployeeCount}" FontWeight="Medium"/>
                    </TextBlock>
                </StackPanel>

                <!-- Refresh Button -->
                <Button Grid.Column="1" 
                      Style="{StaticResource MaterialDesignRaisedButton}"
                      Command="{Binding RefreshCommand}"
                      Content="LÀM MỚI"
                      materialDesign:ButtonAssist.CornerRadius="16"
                      Height="36"
                      Padding="20,0">
                    <Button.IsEnabled>
                        <Binding Path="IsLoading" Converter="{StaticResource InverseBooleanConverter}"/>
                    </Button.IsEnabled>
                </Button>
            </Grid>
        </materialDesign:Card>

        <!-- Search and Filter Bar -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16" materialDesign:ElevationAssist.Elevation="Dp1">
            <StackPanel Orientation="Vertical">
                <!-- Search Box -->
                <TextBox materialDesign:HintAssist.Hint="Tìm kiếm theo tên hoặc mã nhân viên..."
                       materialDesign:HintAssist.IsFloating="False"
                       materialDesign:TextFieldAssist.HasClearButton="True"
                       Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                       Style="{StaticResource MaterialDesignOutlinedTextBox}"
                       Margin="0,0,0,16">
                    <materialDesign:TextFieldAssist.LeadingIcon>
                        <materialDesign:PackIcon Kind="Magnify"/>
                    </materialDesign:TextFieldAssist.LeadingIcon>
                </TextBox>

                <!-- Department Filters -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Bộ phận:" 
                             VerticalAlignment="Center" 
                             Margin="0,0,16,0"
                             FontWeight="Medium"
                             FontSize="13"/>
                    
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden">
                        <ItemsControl ItemsSource="{Binding DepartmentFilters}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}"
                                          Command="{Binding DataContext.SelectDepartmentFilterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          CommandParameter="{Binding}"
                                          Style="{StaticResource DepartmentFilterButtonStyle}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

        <!-- Employee List -->
        <Grid Grid.Row="2">
            <!-- Loading Indicator -->
            <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ProgressBar IsIndeterminate="True" 
                           Margin="0,50,0,0"
                           VerticalAlignment="Top"/>
                <TextBlock Text="Đang tải danh sách nhân viên..."
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         FontSize="14"
                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </Grid>

            <!-- Employee Grid -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                        Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <ItemsControl ItemsSource="{Binding FilteredEmployees}" 
                            ItemTemplate="{StaticResource EmployeeItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="{Binding GridColumns}" 
                                       HorizontalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <!-- Empty State -->
            <StackPanel HorizontalAlignment="Center" 
                      VerticalAlignment="Center"
                      Visibility="{Binding HasNoEmployees, Converter={StaticResource BooleanToVisibilityConverter}}">
                <materialDesign:PackIcon Kind="AccountOff" 
                                       Width="64" Height="64"
                                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                                       HorizontalAlignment="Center"/>
                <TextBlock Text="Không tìm thấy nhân viên nào"
                         Margin="0,16,0,8"
                         HorizontalAlignment="Center"
                         FontSize="16"
                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                <TextBlock Text="Hãy thử thay đổi điều kiện tìm kiếm hoặc làm mới danh sách"
                         HorizontalAlignment="Center"
                         FontSize="12"
                         Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3" 
              Background="{DynamicResource MaterialDesignDivider}"
              Height="1"
              Margin="0,16,0,0"/>
              
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                     VerticalAlignment="Center"
                     FontSize="12"
                     Foreground="{DynamicResource MaterialDesignBodyLight}">
                <Run Text="Cập nhật lần cuối:"/>
                <Run Text="{Binding LastRefreshTime, StringFormat='{}{0:dd/MM/yyyy HH:mm:ss}'}"/>
            </TextBlock>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                      Command="{Binding ExportCommand}"
                      Content="XUẤT DANH SÁCH"
                      Height="32"
                      Padding="16,0"
                      Margin="8,0,0,0">
                    <Button.IsEnabled>
                        <MultiBinding Converter="{StaticResource BooleanAndConverter}">
                            <Binding Path="HasEmployees"/>
                            <Binding Path="IsLoading" Converter="{StaticResource InverseBooleanConverter}"/>
                        </MultiBinding>
                    </Button.IsEnabled>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>